file = fopen("test-save-map.ici.dat");
o = restore(file);
close(file);
if (typeof(o) != "map")
    testutil.failure("restored a %s, expected a map", typeof(o));
k = call(set, keys(o));
if (!k["x"])
    testutil.failure("not \"x\" key");
if (!k["y"])
    testutil.failure("not \"y\" key");
