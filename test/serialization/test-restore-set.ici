file := fopen(test.data_file());
o := restore(file);
close(file);
if (typeof(o) != "set")
    fail("restore failed: " + typeof(o));
if (len(o) != 2)
    fail("restore failed, wrong len");
if (!o["."] || ! o[".."])
    fail("restore failed, elements bad");
