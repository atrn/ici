file := fopen(test.data_file());
o := restore(file);
close(file);
if (typeof(o) != "func")
    fail("restore failed: restored a " + typeof(o));
r := o(2, 4);
if (r != 11)
    fail("restored function gave incorrect result: " + string(r));
