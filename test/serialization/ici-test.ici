export count() {
       return int(argv[1]) || 100;
}

export data_file() {
    return sub(argv[0], #\.ici$#, ".dat");
}

export floateq(a, b) {
    return abs(b - a) < 1e-16;
}

export failure() {
    var vargs = [array];
    fail(call(sprintf, vargs));
}
