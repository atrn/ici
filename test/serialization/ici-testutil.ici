export data_file_name(test) {
       return sub(test, #\.ici#, ".dat");
}

export floateq(a, b) {
    return abs(b - a) < 1e-16;
}

export failure() {
    var vargs = [array];
    fail(call(sprintf, vargs));
}
       