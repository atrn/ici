/*
 * Any strings listed in this file may be refered to
 * with ICIS(name) for a (string_t *), and ICISO(name) for
 * an (object_t *).
 *
 * This file is included with variying definitions
 * of ICI_STR() to declare, define, and initialise
 * the strings.
 */
ICI_STR(tcp, "tcp")
ICI_STR(tcp_ip, "tcp/ip")
ICI_STR(udp, "udp")
ICI_STR(udp_ip, "udp/ip")
ICI_STR(socket, "socket")
ICI_STR(msg, "msg")
ICI_STR(addr, "addr")
ICI_STR(read, "read")
ICI_STR(write, "write")
ICI_STR(n, "n")
ICI_STR(except, "except")

