/*
 * Define a native code function
 */

[cfunc myfunc
{
    long int a;
    long int b;

    // This comment should be removed
    if (ici_typecheck("ii", &a, &b))
    {
        return 1;
    }
    return ici_int_ret(a + b);
    /* Another comment * that should be * removed */
    /* C multiline comments /* don't nest */
}];


printf("%d\n", myfunc(1, 1));
