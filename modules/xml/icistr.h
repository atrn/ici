/*
 * Any strings listed in this file may be refered to
 * with ICIS(name) for a (string_t *), and ICISO(name) for
 * an (object_t *).
 *
 * This file is included with variying definitions
 * of ICI_STR() to declare, define, and initialise
 * the strings.
 */
ICI_STR(parser_class, "parser_class")
ICI_STR(XML_Parser, "XML_Parser")
ICI_STR(start_element, "start_element")
ICI_STR(end_element, "end_element")
ICI_STR(character_data, "character_data")
ICI_STR(default_handler, "default_handler")
ICI_STR(processing_instruction, "processing_instruction")

